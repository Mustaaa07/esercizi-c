#include <stdio.h>

int main(int argc, char *argv[]) {
    int n;
    
    do{
        printf("inserisci n: ");
        scanf("%d",&n);
        
    }while(n<0);
    
    for(int i=n-1; i>=0 ;i--){
        printf("%d \n",i);
    }
    
    return 0;
}
