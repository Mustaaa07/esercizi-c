#include <stdio.h>

int main(int argc, char *argv[])
{

	int n, successivo;

	do
	{									   
		printf("inserisci un numero: \n"); 
		scanf("%d", &n);				   
	} while (n <= 0);					   

	successivo = n + 1;
	printf("il successivo vale: %d\n\n", successivo); 

	return 0;
}
